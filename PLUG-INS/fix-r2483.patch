Index: MODELES/LMDZ.GENERIC/libf/dynphy_lonlat/phystd/lect_start_archive.F
===================================================================
--- MODELES/LMDZ.GENERIC/libf/dynphy_lonlat/phystd/lect_start_archive.F	(révision 2483)
+++ MODELES/LMDZ.GENERIC/libf/dynphy_lonlat/phystd/lect_start_archive.F	(copie de travail)
@@ -973,7 +973,7 @@
 #endif
       IF (ierr .NE. NF_NOERR) THEN
          PRINT*, "lect_start_archive: Failed loading <du_nonoro_gwd>"
-         CALL abort
+!        CALL abort
       ENDIF
 
       write(*,*)"lect_start_archive: loading dv_nonoro_gwd"
@@ -990,7 +990,7 @@
 #endif
       IF (ierr .NE. NF_NOERR) THEN
          PRINT*, "lect_start_archive: Failed loading <dv_nonoro_gwd>"
-         CALL abort
+!        CALL abort
       ENDIF
 
       write(*,*)"lect_start_archive: loading east_gwstress"
@@ -1007,7 +1007,7 @@
 #endif
       IF (ierr .NE. NF_NOERR) THEN
          PRINT*, "lect_start_archive: Failed loading <east_gwstress>"
-         CALL abort
+!        CALL abort
       ENDIF
 
       write(*,*)"lect_start_archive: loading west_gwstress"
@@ -1024,7 +1024,7 @@
 #endif
       IF (ierr .NE. NF_NOERR) THEN
          PRINT*, "lect_start_archive: Failed loading <west_gwstress>"
-         CALL abort
+!        CALL abort
       ENDIF
 
 !=======================================================================
